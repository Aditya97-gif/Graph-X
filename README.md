# Graph-X
A python code that takes input a value of n and than prints the graph x to the power of the input variable. Numpy and matplotlib.pyplot is used to print the graph and the window for the graph is created using argparse library. This code represents the integration of python with statics and math.
